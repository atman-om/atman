# Kubernetes Deployment Notes

This repo includes starter manifests under `infra/k8s/`. They are intentionally conservative and should be wired to managed Postgres, object storage, Redis, Qdrant, and a separately deployed Qwen server for production.
