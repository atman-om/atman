async function loadData() { const res = await fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL || 'http://localhost:8000'}/qwen/serving/status`, { cache: 'no-store' }); if (!res.ok) return null; return res.json(); }
export default async function Page() { const data = await loadData(); return <section><h1>Qwen Serving</h1><p>Deploy or connect Qwen through deterministic/vLLM/Ollama/OpenAI-compatible profiles.</p><div className="card"><pre>{JSON.stringify(data, null, 2)}</pre></div></section>; }
